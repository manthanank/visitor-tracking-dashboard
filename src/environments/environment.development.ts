export const environment = {
    production: true,
    apiUrl: window.location.hostname === 'localhost' ? 'https://visitor-tracking-api.vercel.app/api' : 'https://visitor-tracking-api.vercel.app/api'
};
